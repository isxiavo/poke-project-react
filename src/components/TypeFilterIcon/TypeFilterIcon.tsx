import React, { FC } from 'react';


interface TypeFilterIconProps {}

const TypeFilterIcon: FC<TypeFilterIconProps> = () => (
  <div>
    TypeFilterIcon Component
  </div>
);

export default TypeFilterIcon;
